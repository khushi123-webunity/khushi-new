<div class="progress--bar">
  Rs.00 <progress max="20000" value="{{ cart.total_price }}"></progress> Rs.200
  <br>{% if cart.items.size == 0 %}
    Add items to your cart to receive free shipping.
    {% endif %}
    {% if cart.items.size != 0 %}
      {% if cart.total_price >= 10000 %}
      You've got free shipping 👌
        {% elsif cart.total_price < 10000 %}
        You're only <strong>{{ 10000 | minus: cart.total_price | money }}</strong>
        away from free shipping.
        {% endif %}
      <br>{% if cart.total_price >= 20000 %}
      Congrats... You've got free Gift 🎁
        {% elsif cart.total_price < 20000 %}
        You're only <strong>{{ 20000 | minus: cart.total_price | money }}</strong>
        away from free mystrey Gift.
        {% endif %}
      {% endif %}
 </div>

<style>
  .progress--bar {
    text-align: center;
    margin: 0 auto;
    / width: 100%; /
}
</style>

